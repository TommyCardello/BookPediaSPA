'use strict';

describe('myApp.authorsBio module', function() {

  beforeEach(module('myApp.authorsBio'));

  describe('authorsBio controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var authorsBioCtrl = $controller('authorsBioCtrl');
      expect(authorsBioCtrl).toBeDefined();
    }));

  });
});