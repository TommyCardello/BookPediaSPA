'use strict';

describe('myApp.booksBio module', function() {

  beforeEach(module('myApp.booksBio'));

  describe('booksBio controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var booksBioCtrl = $controller('booksBioCtrl');
      expect(booksBioCtrl).toBeDefined();
    }));

  });
});