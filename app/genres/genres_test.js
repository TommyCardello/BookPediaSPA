'use strict';

describe('myApp.genres module', function() {

  beforeEach(module('myApp.genres'));

  describe('genres controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var genresCtrl = $controller('genresCtrl');
      expect(genresCtrl).toBeDefined();
    }));

  });
});